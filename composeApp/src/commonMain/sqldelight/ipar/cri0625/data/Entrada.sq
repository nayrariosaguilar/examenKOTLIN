CREATE TABLE IF NOT EXISTS Entrada (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    butaca INTEGER,
    sessio INTEGER,
     FOREIGN KEY(butaca) REFERENCES Butaca(numero),
     FOREIGN KEY(sessio) REFERENCES Session(dia)
);
insert:
INSERT INTO Entrada(butaca, sessio) VALUES (?,?);
select:
SELECT * FROM Entrada;